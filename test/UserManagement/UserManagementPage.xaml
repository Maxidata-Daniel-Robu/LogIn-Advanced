<Page x:Class="test.UserManagement.UserManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="User Management"
      Background="#FF1E1E1E">

    <!-- DataContext is set in code-behind -->

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Stretch">

            <!-- Search Bar -->
            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     Height="32" Margin="0,0,0,10"
                     FontSize="16" Background="#FF2E2E2E"
                     Foreground="White" Padding="5"
                     BorderBrush="Gray" BorderThickness="1"/>

            <!-- DataGrid -->
            <DataGrid ItemsSource="{Binding FilteredUsers}"
                      SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                      AutoGenerateColumns="False" CanUserAddRows="False"
                      Background="#FF2D2D30" Foreground="White"
                      GridLinesVisibility="None"
                      RowBackground="#FF3E3E42"
                      AlternatingRowBackground="#FF2D2D30"
                      HeadersVisibility="Column"
                      SelectionMode="Single" SelectionUnit="FullRow"
                      IsReadOnly="False" FontSize="14" Height="400">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FF1E1E1E"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Foreground"  Value="White"/>
                        <Setter Property="FontSize"    Value="14"/>
                        <Setter Property="BorderBrush" Value="#444"/>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding Id}"
                                        Width="50"  IsReadOnly="True"/>
                    <DataGridTextColumn Header="Hashed Password"
                                        Binding="{Binding HashedPassword}"
                                        Width="*"   IsReadOnly="True"/>
                    <DataGridTextColumn Header="Description"
                                        Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                        Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Refresh"
            Command="{Binding RefreshCommand}"
            Width="100" Height="36"
            Background="#FF007ACC" Foreground="White"
            Margin="0,0,8,0"/>

                <Button Content="Save Descriptions"
            Command="{Binding SaveDescriptionsCommand}"
            Width="150" Height="36"
            Background="#FF007ACC" Foreground="White"/>
            </StackPanel>

            <!-- Selected Username -->
            <TextBlock Text="{Binding SelectedUsername}"
                       FontWeight="Bold"
                       Foreground="LightSteelBlue"
                       FontSize="16"
                       Margin="0,15,0,0"/>
        </StackPanel>
    </Grid>
</Page>
